{
  "name": "ahc-control-centre",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --test-scheduled",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply AHC_ANALYTICS_DB --local",
    "db:migrate:remote": "wrangler d1 migrations apply AHC_ANALYTICS_DB --remote",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check src/index.js && node --check src/auth.js && node --check src/db.js && node --check src/google.js && node --check src/sync.js && node --check public/app.js && npm test"
  },
  "devDependencies": {
    "wrangler": "^4.28.1"
  }
}
